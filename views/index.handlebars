{{!-- Create the reder file here --}}
<h1>Burgers</h1>

<h2>Add a Burger!</h2>
<form class="create-form">
    <div class="form-group">
        <label for="burger-name" id="title">Burger Name:</label>
        <input type="text" id="burger-name" name="name">
    </div>
    <button class="button" id="btn" type="dubmit">Add New Burger</button>
</form>

<h2>Burgers that have yet to be devoured:</h2>

<ul>
    {{#each burgers}}
        {{#unless devoured}}
            {{> burgers/devour devoured=false}}
        {{/unless}}
    {{/each}}
</ul>

<h2>Burgers that have been devoured:</h2>

<ul>
    {{#each burgers}}
        {{#if devoured}}
            {{> burgers/devour devoured=true}}
        {{/if}}
    {{/each}}
</ul>